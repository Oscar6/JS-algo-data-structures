// TypeOf Boolean

function booWho(bool) {
// Using operator typeof to check if the variable is a boolean. 
// If boolean, it will return true. Otherwise, return false.
    return typeof bool === 'boolean';
}

booWho(null);

// test